<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Clarity by Example</title>
    <link rel=stylesheet href="site.css">
  </head>
  <body>
    <div id="intro">
      <h2><a href="./" id="logo">Clarity by Example (WIP)</a></h2>
      <p>
        <a href="https://clarity-lang.org/">Clarity</a> is an interpreted programming language bringing smart contracts to bitcoin currently through the stacks blockchain. 
      </p>

      <p>
        <em>Clarity by Example</em> is a hands-on introduction
        to Clarity using annotated example smart contracts. Check out
        the <a href="hello-world">first example</a> or
        browse the full list below.
      </p>

      <ul>
      {{range .}}
        <li><a href="{{.ID}}">{{.Name}}</a></li>
      {{end}}
      </ul>
{{ template "footer" }}
    </div>
  </body>
</html>
